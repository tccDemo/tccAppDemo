export class Bookmark {
  id: string;
  subject: string;
  description: string;
  link: string;
  imgIcon: string;
  iconType: string;
  iconCode: string;
  iconBg: string;
  iconFg: string;
  isMyFavour: boolean;
  isRecent: boolean; // just for fake data
  isPopular: boolean; // just for fake data
  seq: number;
  iconURL: string;
}